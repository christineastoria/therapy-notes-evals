[project]
name = "therapy-notes-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3",
    "langchain-openai>=0.3",
    "langsmith>=0.2",
    "python-dotenv>=1.0",
]
